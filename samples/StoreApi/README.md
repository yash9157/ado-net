# StoreApi

Run from the repository root after starting LocalDB:

```powershell
dotnet run --project samples/StoreApi/StoreApi.csproj --urls http://localhost:5000
```

The Development startup applies the migrations automatically. Follow [the API walkthrough](../../docs/web-api-example.md) to register, login, and call the product endpoints.
